#include <bits/stdc++.h>

using namespace std;

vector<int> longest_band(vector<int> &nums)
{
    unordered_set<int> set;

    for (int a : nums)
        set.insert(a);

    for (int a : nums)
    {
    }
}

int main(int argc, char const *argv[])
{
    /* code */
    return 0;
}
